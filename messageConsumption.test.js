// const messageConsumer = require('./messageConsumption')
//
// test('it can handle empty database', () => {
//   // the params 'true' and 'WORDS' only need to be passed in for first test, to initialise module vars.
//   expect(messageConsumer('').toBe(''))
// })